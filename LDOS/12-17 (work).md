to run YCSB, first cd into My-YCSB 

there are some libraries needed to compile YCSB
```bash
sudo apt install autoconf automake libtool
```

This is needed to run
```bash
sudo apt install cgroup-tools
```

Some python deps
```bash
pip install psutil
pip install ruamel.yaml
```